buildscript {
    // redeclared in subprojects kts because I don't figure out how to use it there
    ext.kotlinVersion = "1.8.10"
    ext.kotlinxSerializationVersion = "1.5.0"
    ext.ktorVersion = "2.2.4"
    ext.navigationVersion = "2.5.3"
    ext.hiltVersion = "2.45"
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
